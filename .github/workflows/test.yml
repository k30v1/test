on:
  push:
    tags:
      - "*"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: pip install numpy
    - run: python -c "import requests; print(numpy.__version__)"
    - run: python -c "import os;print(os.getcwd(),os.listdir())"
