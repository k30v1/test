on:
  push:
    branches:
      - "*"

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
    - uses: actions/checkout@v4
    - run: python -c "import os;print(os.getcwd(),os.listdir(),os.environ)"
